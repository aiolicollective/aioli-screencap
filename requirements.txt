# Minimum versions, no exact pin: pip picks the most recent
# prebuilt package that matches your Python.
# setup.bat installs with --only-binary=:all: : nothing is ever
# compiled on your machine (no build code runs).
mss>=9.0
Pillow>=10.3
