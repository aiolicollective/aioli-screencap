# Versions minimales, pas de version exacte : pip prend le paquet
# precompile le plus recent compatible avec ton Python.
# setup.bat installe avec --only-binary=:all: : rien n'est jamais
# compile sur ta machine (pas de code de build execute).
mss>=9.0
Pillow>=10.3
